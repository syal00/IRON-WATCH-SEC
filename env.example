# Email Configuration
# Required for production. In development, if these are not set, form submissions will be logged to console instead.

# SMTP Server Configuration
# Example for Gmail (you may need to use an App Password):
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Recipient Email Address
# The email address where contact form submissions will be sent
CONTACT_TO=ironwatchsec.services@gmail.com

# Next.js Configuration
NODE_ENV=development
